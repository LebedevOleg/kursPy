from tkinter import *
import random
import msvcrt
import time
import os
from Elevator import*
from Floor import*
from People import*
def butt2(root,numF,numE):
    build2 = Toplevel(root)
    build2.minsize(width=600,height=700)
    с = Canvas(build2,width = 600,height = 700,bg = 'white')
    c.pack()
